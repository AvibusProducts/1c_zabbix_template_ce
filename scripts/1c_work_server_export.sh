#!/bin/bash
#
# Мониторинг 1С Предприятия 8.3 (рабочий сервер)
#
#set -x

WORK_DIR=$(dirname "${0}" | sed -r 's/\\/\//g; s/^(.{1}):/\/\1/')

timeout 10s "${WORK_DIR}/1c_work_server.sh" "${@}" || { echo "Выполнение команды прервано по достижении таймаута" ; exit 1; }
